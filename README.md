Gen AI Recruitment Tasks — Windows scaffold

This repository contains three example tools:
- task1_video_generator — generate short videos from web assets
- task2_seo_blog_tool — generate and publish SEO-optimized blog posts
- task3_architecture_pipeline — minimal web API and Vercel deployment config

Run `python setup.py` to create a .env from environment keys.
See each task's README for usage.
